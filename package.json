{
  "name": "campusos-root",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist || xcopy /E /I /Y dist ..\\dist || echo 'built'",
    "start": "cd frontend && npm run preview"
  }
}
